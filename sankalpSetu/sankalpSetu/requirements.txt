Django>=5.1.3

# Django REST Framework
djangorestframework
djangorestframework-simplejwt

# CORS headers for API
django-cors-headers

# For JSONField (if using older Django, but 5.x supports it natively)
# psycopg2  # Uncomment if using PostgreSQL

# Pillow (if you use ImageField anywhere)
# Pillow

# Any other common dependencies can be added here 